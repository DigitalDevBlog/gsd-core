---
phase: 02-the-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/research/existing-docs.md
  - learn/loop/index.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Establish what the repo already documents (so this site reverse-engineers source rather
than paraphrasing existing docs), then write the Loop overview page: GSD Core's
spec-driven cycle as a construction, with a diagram.

## Purpose
The loop is the framework's control flow. Every later section attaches to it. Getting the
recon right first protects the project's stated success metric — every explanation
traceable to real source.

## Output
- `.paul/research/existing-docs.md` — prior-art map, reused by all later phases
- `learn/loop/index.md` — the Loop overview with a PlantUML diagram
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior art that MUST be reconnoitred first
- docs/ARCHITECTURE.md (990 lines)
- docs/INVENTORY.md (689 lines)
- docs/explanation/the-phase-loop.md  ← directly overlaps this phase
- docs/explanation/context-engineering.md

## Primary source (the actual subject)
- skills/gsd-new-project/, skills/gsd-plan-phase/, skills/gsd-execute-phase/,
  skills/gsd-verify-work/, skills/gsd-complete-milestone/ (each a SKILL.md)
- commands/gsd/ (namespaced subcommand files)
</context>

<acceptance_criteria>

## AC-1: Prior art mapped before writing
```gherkin
Given the repo's existing docs/ tree
When recon completes
Then .paul/research/existing-docs.md lists what is already documented, and flags any
     place the existing docs disagree with the source files
```

## AC-2: Loop overview explains construction, not usage
```gherkin
Given a reader who wants to build a similar framework
When they read learn/loop/index.md
Then they learn what each stage produces, why the cycle is closed, and what design
     pressure each stage answers — not how to run the commands
```

## AC-3: Every structural claim is traceable
```gherkin
Given the finished page
When any structural claim is checked
Then it cites a real repo path, and that path exists
```

## AC-4: Site still builds strict-clean
```gherkin
Given new pages added under learn/
When `mkdocs build --strict` runs
Then it exits 0 with every page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Reconnoitre existing documentation</name>
  <files>.paul/research/existing-docs.md</files>
  <action>
    Read docs/ARCHITECTURE.md, docs/INVENTORY.md, and docs/explanation/*.md. Produce a
    map: what each already covers, which claims are load-bearing for this site, and any
    disagreement between those docs and the actual source files.
    Avoid: treating the existing docs as ground truth — source wins on conflict.
  </action>
  <verify>File exists and names specific files with specific findings</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Write the Loop overview page</name>
  <files>learn/loop/index.md, mkdocs.yml</files>
  <action>
    Rewrite learn/loop/index.md (currently a placeholder) as the Loop overview:
    the stages, what each reads and writes, why the cycle closes rather than running once,
    and a kroki-plantuml diagram of the cycle. Cite repo paths for structural claims.
    Update mkdocs.yml nav only if new pages are added.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; page cites paths that exist</verify>
  <done>AC-2 + AC-3 + AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/**, skills/**, agents/**, hooks/**, capabilities/**, bin/**, commands/**, gsd-core/**, docs/**, .plans/**
- package.json, .gitignore
- Phase 1 foundation config beyond nav additions

## SCOPE LIMITS
- Overview only — per-stage deep dives are plan 02-02
- No content for other sections
</boundaries>

<verification>
- [ ] .paul/research/existing-docs.md exists with specific findings
- [ ] learn/loop/index.md replaces the placeholder
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- Prior art mapped and reusable by later phases
- Loop overview reads as construction, not manual
</success_criteria>

<output>
After completion, create `.paul/phases/02-the-loop/02-01-SUMMARY.md`
</output>
