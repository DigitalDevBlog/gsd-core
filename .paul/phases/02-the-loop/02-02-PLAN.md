---
phase: 02-the-loop
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - learn/loop/stages.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Per-stage deep dives: how planning, execution and verification are each constructed as
model-executable specifications, grounded in the real SKILL.md sources.

## Purpose
The overview says what the cycle is. This page shows how a stage is actually built — the
level a reader needs to reconstruct one.

## Output
- `learn/loop/stages.md` — deep dive per stage, with cited source paths
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md
@.paul/phases/02-the-loop/02-01-SUMMARY.md

## Primary source
- skills/gsd-plan-phase/SKILL.md
- skills/gsd-execute-phase/SKILL.md
- skills/gsd-verify-work/SKILL.md
- skills/gsd-new-project/SKILL.md
</context>

<acceptance_criteria>

## AC-1: Each stage explained as a built artifact
```gherkin
Given learn/loop/stages.md
When a reader studies any stage section
Then they see how that stage's specification is structured and what makes it executable
     by a model without a runtime interpreter
```

## AC-2: Claims cite real source
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Strict build passes with the new page in nav
```gherkin
Given learn/loop/stages.md added
When `mkdocs build --strict` runs
Then exit 0, page present in nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the per-stage deep dive</name>
  <files>learn/loop/stages.md, mkdocs.yml</files>
  <action>
    For each of plan / execute / verify (and project bootstrap), explain how the SKILL.md
    is structured, what it instructs the model to produce, how state is handed to the next
    stage, and what design decision that encodes. Cite path references for each claim.
    Add the page to mkdocs.yml nav under "The Loop".
    Avoid: repeating the overview; avoid usage instructions.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- The loop only — other strata belong to phases 3-9
</boundaries>

<verification>
- [ ] learn/loop/stages.md exists and is substantive
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could reconstruct the shape of a stage specification from this page
</success_criteria>

<output>
After completion, create `.paul/phases/02-the-loop/02-02-SUMMARY.md`
</output>
