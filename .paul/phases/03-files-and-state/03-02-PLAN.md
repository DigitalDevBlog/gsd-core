---
phase: 03-files-and-state
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - learn/files/dataflow.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Document how state actually moves: the roadmap/ledger machinery in code, and a
cross-stage data-flow diagram showing what each stage reads and writes.

## Purpose
The artifact catalogue says what exists. This shows the wiring — the part you must get
right for a loop to survive interruption and resume correctly.

## Output
- `learn/files/dataflow.md` — data-flow diagram + the parsing/consistency machinery
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/03-files-and-state/03-01-SUMMARY.md

## Primary source
- src/roadmap-parser.cts, src/roadmap.cts, src/roadmap-upgrade.cts
- src/planning-workspace.cts, src/planning-snapshot.cts, src/planning-scope.cts,
  src/planning-inspect.cts
- src/health-diagnostic-rules/roadmap-disk-consistency.cts
</context>

<acceptance_criteria>

## AC-1: Data flow is shown, not just described
```gherkin
Given learn/files/dataflow.md
When a reader opens it
Then a kroki-plantuml diagram shows which stage reads and writes which artifact
```

## AC-2: The consistency machinery is explained
```gherkin
Given the page
When a reader asks how the framework keeps files and reality in sync
Then the page explains the parsing and disk-consistency checks, citing real .cts modules
```

## AC-3: Traceable and building
```gherkin
Given the finished page
When paths are checked and `mkdocs build --strict` runs
Then every cited path exists and the build exits 0
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the data-flow page</name>
  <files>learn/files/dataflow.md, mkdocs.yml</files>
  <action>
    Explain how roadmap state is parsed and kept consistent, what a snapshot is for, and
    how the loop survives interruption. Include a kroki-plantuml data-flow diagram.
    Add the page to mkdocs.yml nav under "Files & State".
    Avoid: pasting code; explain structure and intent, cite paths.
  </action>
  <verify>`mkdocs build --strict` exits 0; cited paths resolve</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source, package.json, .gitignore

## SCOPE LIMITS
- State machinery only — the broader runtime is phase 6
</boundaries>

<verification>
- [ ] Diagram present
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could design an equivalent state layer for their own framework
</success_criteria>

<output>
After completion, create `.paul/phases/03-files-and-state/03-02-SUMMARY.md`
</output>
