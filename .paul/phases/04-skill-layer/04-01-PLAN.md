---
phase: 04-skill-layer
plan: 01
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - learn/skills/index.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Document what a GSD skill file actually is — its frontmatter contract, body structure, and how one namespaced command surface routes into 71 of them.

## Purpose
The skill layer is where GSD Core turns markdown into executable behaviour. It is the single most copyable idea in the framework.

## Output
- `learn/skills/index.md` — skill anatomy and dispatch
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- skills/gsd-plan-phase/SKILL.md (frontmatter: name, description, argument-hint, allowed-tools)
- skills/gsd-execute-phase/SKILL.md, skills/gsd-help/SKILL.md
- commands/gsd/ (71 namespaced subcommand files)
- src/agent-command-router.cts, src/planning-command-router.cts
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the skill anatomy + routing page</name>
  <files>learn/skills/index.md, mkdocs.yml</files>
  <action>
    Explain the SKILL.md frontmatter contract field by field (name, description,
    argument-hint, allowed-tools) and what each buys. Explain how commands/gsd/ maps onto
    skills and how the routers dispatch. Show what makes a markdown file executable by a
    model with no interpreter.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Anatomy and routing only — the catalogue is plan 04-02
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could design their own skill-file format and dispatch layer
</success_criteria>

<output>
After completion, create `.paul/phases/04-skill-layer/01-SUMMARY.md`
</output>
