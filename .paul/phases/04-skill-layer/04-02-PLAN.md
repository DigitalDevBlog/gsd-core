---
phase: 04-skill-layer
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - learn/skills/catalogue.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Map the 71 skills by role and loop position, showing how a large skill surface stays navigable.

## Purpose
A framework with 71 skills needs an organising principle or it collapses into sprawl. The grouping IS the design.

## Output
- `learn/skills/catalogue.md` — skills grouped by role and loop position
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- skills/ (71 skill directories, each with SKILL.md)
- commands/gsd/ (71 files)
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the skill catalogue</name>
  <files>learn/skills/catalogue.md, mkdocs.yml</files>
  <action>
    Group the 71 skills by role (planning, execution, verification, diagnostics, meta) and
    by loop position. For each group explain what the group exists to do and name
    representative skills. Draw the organising principle out explicitly.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Cataloguing and grouping only
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader sees how to keep a large skill surface coherent
</success_criteria>

<output>
After completion, create `.paul/phases/04-skill-layer/02-SUMMARY.md`
</output>
