---
phase: 05-agent-layer
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - learn/agents/index.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Document the agent definition format: frontmatter contract, tool scoping, and what an agent promises its caller.

## Purpose
Subagents are how GSD Core buys parallelism and context isolation. The contract design is what makes them safe to spawn.

## Output
- `learn/agents/index.md` — agent anatomy and contracts
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- agents/gsd-executor.md, agents/gsd-planner.md, agents/gsd-verifier.md,
  agents/gsd-doc-writer.md, agents/gsd-doc-verifier.md
- gsd-core/references/agent-contracts.md
- src/agent-command-router.cts, src/agent-install-check.cts
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the agent anatomy page</name>
  <files>learn/agents/index.md, mkdocs.yml</files>
  <action>
    Explain the agent frontmatter (name, description, tools, color), least-privilege tool
    scoping and why it matters, and the contract an agent fulfils for its caller. Ground
    it in gsd-core/references/agent-contracts.md.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Anatomy and contracts only — orchestration patterns are plan 05-02
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could specify their own subagent contract format
</success_criteria>

<output>
After completion, create `.paul/phases/05-agent-layer/01-SUMMARY.md`
</output>
