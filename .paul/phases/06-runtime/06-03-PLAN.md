---
phase: 06-runtime
plan: 03
type: execute
wave: 3
depends_on: ["06-01"]
files_modified:
  - learn/runtime/bootstrap.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Document the MCP server and the installer — how the framework gets into a harness and exposes itself as tools.

## Purpose
Distribution and bootstrap are the unglamorous parts that decide whether a framework is usable by anyone but its author.

## Output
- `learn/runtime/bootstrap.md` — MCP server + installer/bootstrap
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- bin/install.js, bin/gsd-mcp-server.js
- gsd-core/bin/ (gsd-tools.cjs, gsd_run)
- package.json (bin entries, files array)
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the MCP + installer page</name>
  <files>learn/runtime/bootstrap.md, mkdocs.yml</files>
  <action>
    Explain what the installer copies where and why the layout matters, then how the MCP
    server exposes framework operations as callable tools. Cover the packaging decisions
    visible in package.json (bin entries, files array).
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Bootstrap and distribution only
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could package and distribute their own framework
</success_criteria>

<output>
After completion, create `.paul/phases/06-runtime/03-SUMMARY.md`
</output>
