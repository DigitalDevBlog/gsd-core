---
phase: 07-capabilities
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - learn/capabilities/index.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Document capability anatomy and lifecycle: what a capability pack is, how it is loaded, activated, and trusted.

## Purpose
46 capability packs is a plugin system. How it handles trust and activation is the transferable part.

## Output
- `learn/capabilities/index.md` — capability anatomy, lifecycle, trust
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- capabilities/ (46 packs; capabilities/claude/capability.json as the shape)
- src/capability-loader.cts, capability-activation.cts, capability-consent.cts,
  capability-trust.cts, capability-lifecycle.cts, capability-ledger.cts, capability-lock.cts
- docs/explanation/capability-trust-model.md, docs/explanation/capability-overlay-model.md (prior art)
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the capability anatomy page</name>
  <files>learn/capabilities/index.md, mkdocs.yml</files>
  <action>
    Explain the capability.json shape, the load→activate→consent→lock lifecycle, and the
    trust model. Ground every stage in the corresponding .cts module.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Anatomy and lifecycle — harness adapters are 07-02
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could design a trusted plugin system for their own framework
</success_criteria>

<output>
After completion, create `.paul/phases/07-capabilities/01-SUMMARY.md`
</output>
