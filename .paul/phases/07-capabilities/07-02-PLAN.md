---
phase: 07-capabilities
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - learn/capabilities/harnesses.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Document the multi-harness strategy: how one framework targets Claude, Cursor, Codex, Gemini, Kilo, Kimi, Windsurf and Copilot.

## Purpose
Portability across agent harnesses is a design problem with real costs. GSD Core's answer is worth studying before you pick your own.

## Output
- `learn/capabilities/harnesses.md` — adapters and the portability strategy
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- src/adapter-declarative.cts, src/adapter-imperative.cts
- capabilities/{claude,cursor,codex,gemini,kilo,kimi,copilot,antigravity}/
- hooks/gsd-cursor-*.js, hooks/gsd-windsurf-*.js (per-harness hook variants)
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the multi-harness page</name>
  <files>learn/capabilities/harnesses.md, mkdocs.yml</files>
  <action>
    Contrast the declarative and imperative adapter approaches, show what varies per
    harness (hooks, command formats, tool names) and what stays shared. Be honest about
    the maintenance cost this design carries.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Adapters and portability only
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader can decide whether multi-harness support is worth it for their framework
</success_criteria>

<output>
After completion, create `.paul/phases/07-capabilities/02-SUMMARY.md`
</output>
