---
phase: 08-architecture
plan: 01
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - learn/architecture/index.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Assemble every stratum into one picture, with a system diagram and an end-to-end trace of a single real command.

## Purpose
Synthesis is where the separate sections become a mental model a reader can actually hold and reuse.

## Output
- `learn/architecture/index.md` — system diagram + end-to-end walkthrough
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- All prior learn/ pages (loop, files, skills, agents, runtime, capabilities)
- Trace one real command end to end through commands/gsd/, skills/, agents/, src/, hooks/
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the architecture synthesis</name>
  <files>learn/architecture/index.md, mkdocs.yml</files>
  <action>
    Present the strata as one system with a kroki-plantuml diagram, then trace a single
    real command from invocation through routing, skill, agents, runtime and hooks to
    written artifacts. Close with the design principles the architecture embodies and an
    honest note on where the seams are.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Synthesis only — no new primary research
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader holds one coherent mental model of the whole framework
</success_criteria>

<output>
After completion, create `.paul/phases/08-architecture/01-SUMMARY.md`
</output>
