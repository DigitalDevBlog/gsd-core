---
phase: 09-build-your-own
plan: 01
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - learn/build-your-own/index.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Extract the transferable design skills and give an honest reuse / adapt / drop verdict on each of GSD Core's choices.

## Purpose
This is the project's core value made explicit: not what GSD Core did, but what you should do.

## Output
- `learn/build-your-own/index.md` — transferable skills + reuse/adapt/drop guide
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- All prior learn/ pages
- The design decisions recorded across phases 2-8
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the transferable skills guide</name>
  <files>learn/build-your-own/index.md, mkdocs.yml</files>
  <action>
    Name the transferable design skills explicitly, then give a reuse / adapt / drop table
    over GSD Core's major choices (spec-driven loop, file state, skill files, subagents,
    runtime code, hooks, capability packs, multi-harness). Justify each verdict by scale
    and team size — what a solo builder should copy differs from what a team should.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Skills and verdicts — the blueprint is 09-02
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader knows which of GSD Core's choices apply to their situation
</success_criteria>

<output>
After completion, create `.paul/phases/09-build-your-own/01-SUMMARY.md`
</output>
