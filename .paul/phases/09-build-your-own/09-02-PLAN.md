---
phase: 09-build-your-own
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - learn/build-your-own/blueprint.md
  - mkdocs.yml
autonomous: true
---

<objective>
## Goal
Give a minimal, concrete blueprint for a framework of this kind, plus a grow-from-here path.

## Purpose
The payoff page. A reader should finish it able to start building on Monday morning.

## Output
- `learn/build-your-own/blueprint.md` — minimal blueprint + growth path
</objective>

<context>
@.paul/PROJECT.md
@.paul/research/existing-docs.md

## Primary source
- All prior learn/ pages
- The minimal subset identified in 09-01
</context>

<acceptance_criteria>

## AC-1: Explains construction, not usage
```gherkin
Given the finished page
When a reader who wants to build a similar framework studies it
Then they learn how the thing is structured and what design decision it encodes
```

## AC-2: Every structural claim is traceable
```gherkin
Given any structural claim on the page
When the cited path is checked
Then the file exists and supports the claim
```

## AC-3: Site builds strict-clean
```gherkin
Given the new page under learn/
When `mkdocs build --strict` runs
Then it exits 0 with the page reachable from nav
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write the minimal blueprint</name>
  <files>learn/build-your-own/blueprint.md, mkdocs.yml</files>
  <action>
    Specify the smallest framework worth building: one command surface, one skill format,
    a tiny state file, one loop. Show the actual file layout and a sketch of each piece.
    Then give a staged growth path — what to add first, second, third, and the signal
    that tells you it is time.
    Cite real repo paths for structural claims. Add nav entries for any new page.
    Avoid: usage instructions; avoid claims not backed by a file.
  </action>
  <verify>`mkdocs build --strict` exits 0; every cited path resolves</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Framework source (src/, skills/, agents/, hooks/, capabilities/, bin/, commands/, gsd-core/, docs/, .plans/)
- package.json, .gitignore

## SCOPE LIMITS
- Blueprint only
</boundaries>

<verification>
- [ ] Page is substantive, not a stub
- [ ] All cited paths resolve
- [ ] `mkdocs build --strict` exits 0
</verification>

<success_criteria>
- A reader could start building their own framework immediately
</success_criteria>

<output>
After completion, create `.paul/phases/09-build-your-own/02-SUMMARY.md`
</output>
